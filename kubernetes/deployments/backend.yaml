# ---------------------------------------------------------------------------
# Backend Deployment (Flask API)
# - Runs as non-root (UID 1000)
# - Read-only root filesystem with writable /tmp emptyDir
# - No privilege escalation, all Linux capabilities dropped
# - Resource limits enforced
# - Uses IRSA via the app-backend ServiceAccount for AWS Secrets Manager
# ---------------------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: fintech-app
  labels:
    app: backend
    tier: api
    project: fintech-secure
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
        tier: api
        project: fintech-secure
    spec:
      serviceAccountName: app-backend
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: backend
          # Replace 180571024040 with your AWS account ID; image pushed to ECR.
          image: 180571024040.dkr.ecr.us-east-2.amazonaws.com/fintech-secure/backend:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          env:
            - name: AWS_REGION
              value: us-east-2
            - name: APP_SECRETS_NAME
              value: fintech-secure-dev-backend
            - name: DATABASE_URL
              value: postgresql://fintech_app:CHANGE_ME_USE_SECRETS_MANAGER@postgres.fintech-app.svc.cluster.local:5432/fintech
            - name: CORS_ORIGINS
              value: http://localhost:5173,http://localhost:8080
            - name: LOG_LEVEL
              value: INFO
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: backend
                topologyKey: kubernetes.io/hostname
